## AKTUELLE AUFGABE

Intent: {{ intent }}
Konfidenz: {{ confidence }}
Benutzernachricht: "{{ original_message }}"

---

## KONTEXT

| Feld                | Wert                     |
|---------------------|--------------------------|
| Sprache             | {{ language }}           |
| object_id           | {{ object_id }}          |
| current_report_id   | {{ current_report_id }}  |
| previous_report_id  | {{ previous_report_id }} |

> "not_set" bedeutet, dass der Wert nicht angegeben wurde. Behandle es als nicht vorhanden.
> WICHTIG: Kopiere niemals "not_set" oder eine Übersetzung davon in Worker-Parameter. Verwende null für fehlende optionale Felder.

---

## EXTRAHIERTE PARAMETER

```json
{{ extracted_parameters }}
```

Fehlende Kontextfelder laut Router: {{ missing_context }}

---

## BISHER ABGESCHLOSSENE WORKER

{% if worker_results | length == 0 -%}
Keine.
{%- else -%}
{% for result in worker_results %}
- {{ result.worker_type }}: {{ result.status }}
{% endfor %}
{%- endif %}

---

Entscheide die einzige nächste Aktion. Gib nur gültiges JSON zurück.
