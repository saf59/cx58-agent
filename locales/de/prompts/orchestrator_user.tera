## AKTUELLE AUFGABE

Intent: {{ intent }}
Confidence: {{ confidence }}
Benutzeranfrage: "{{ original_message }}"

---

## KONTEXT

| Feld                | Wert                     |
|---------------------|--------------------------|
| Sprache             | {{ language }}           |
| object_id           | {{ object_id }}          |
| current_report_id   | {{ current_report_id }}  |
| previous_report_id  | {{ previous_report_id }} |

> "not_set" bedeutet, dass der Wert nicht bereitgestellt wurde. Behandeln Sie ihn als nicht vorhanden.

---

## EXTRAHIERTE PARAMETER

```json
{{ extracted_parameters }}
```

Fehlende Kontextfelder laut Router: {{ missing_context }}

---

## BISHER AUSGEFÜHRTE WORKER

{% if worker_results | length == 0 -%}
Keine.
{%- else -%}
{% for result in worker_results %}
- {{ result.worker_type }}: {{ result.status }}
{% endfor %}
{%- endif %}

---

Entscheiden Sie die einzelne nächste Aktion. Geben Sie nur gültiges JSON zurück.