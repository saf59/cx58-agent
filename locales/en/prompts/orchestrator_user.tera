## CURRENT TASK

Intent: {{ intent }}
Confidence: {{ confidence }}
User message: "{{ original_message }}"

---

## CONTEXT

| Field               | Value                    |
|---------------------|--------------------------|
| Language            | {{ language }}           |
| object_id           | {{ object_id }}          |
| current_report_id   | {{ current_report_id }}  |
| previous_report_id  | {{ previous_report_id }} |

> "not_set" means the value was not provided. Treat it as absent.

---

## EXTRACTED PARAMETERS

```json
{{ extracted_parameters }}
```

Missing context fields declared by router: {{ missing_context }}

---

## WORKERS COMPLETED SO FAR

{% if worker_results | length == 0 -%}
None.
{%- else -%}
{% for result in worker_results %}
- {{ result.worker_type }}: {{ result.status }}
{% endfor %}
{%- endif %}

---

Decide the single next action. Return valid JSON only.
